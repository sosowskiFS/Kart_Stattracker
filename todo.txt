Seperated time saves for encore mode runs
Put disconnectors/rage speccers into the ELO calculations (toggleable)
Make ELO minimum exchange 1 instead of 0
Remove SPB Attack check, just check to make sure more than 1 player is playing instead