Check realtime in intermission hook to determine position instead of using kartstuff.
Make the ELO Change local table a global table instead for exposure
Hide new record popup drawing during fake intermission
	if (hm_intermissioncalc and hm_intermissioncalc()) then don'tdraw
	Only happens for one tick so set a flag to not draw it